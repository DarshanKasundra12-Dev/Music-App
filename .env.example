# Backend
PORT=5000

# Frontend — set this to your backend's public domain (no https://, no trailing slash)
# Local development example:
EXPO_PUBLIC_DOMAIN=localhost:5000

# Production example (your deployed backend URL):
# EXPO_PUBLIC_DOMAIN=your-app-name.replit.app

# Optional — Node environment
NODE_ENV=development
